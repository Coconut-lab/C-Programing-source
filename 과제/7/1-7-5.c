#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
    int A, B, C, result;
    printf("숫자 3개를 입력하세요: ");
    scanf("%d %d %d", &A, &B, &C);

    result = A * B * C;

    while (result <= 0) {

    }

}