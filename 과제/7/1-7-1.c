#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main(void) {
    int num;

    while (1) {
        printf("숫자를 입력하세요(종료: -1): ");
        scanf("%d", &num);

        if (num == -1) {
            
        }
    }
}